-- Temporarily disable and recreate all policies more simply
-- Categories (public read)
DROP POLICY IF EXISTS "Public can view categories" ON categories;
CREATE POLICY "Public can view categories" ON categories FOR SELECT USING (true);

DROP POLICY IF EXISTS "Admins can do everything on categories" ON categories;
CREATE POLICY "Admins can do everything on categories" ON categories FOR ALL 
USING (auth.uid() IS NOT NULL);

-- Menu items (public read available items)
DROP POLICY IF EXISTS "Public can view menu items" ON menu_items;
CREATE POLICY "Public can view menu items" ON menu_items FOR SELECT USING (true);

DROP POLICY IF EXISTS "Admins can do everything on menu_items" ON menu_items;
CREATE POLICY "Admins can do everything on menu_items" ON menu_items FOR ALL 
USING (auth.uid() IS NOT NULL);

-- Reviews (public read featured)
DROP POLICY IF EXISTS "Public can view featured reviews" ON reviews;
CREATE POLICY "Public can view featured reviews" ON reviews FOR SELECT USING (true);

DROP POLICY IF EXISTS "Admins can do everything on reviews" ON reviews;
CREATE POLICY "Admins can do everything on reviews" ON reviews FOR ALL 
USING (auth.uid() IS NOT NULL);

-- Gallery (public read)
DROP POLICY IF EXISTS "Public can view gallery photos" ON gallery_photos;
CREATE POLICY "Public can view gallery photos" ON gallery_photos FOR SELECT USING (true);

DROP POLICY IF EXISTS "Admins can do everything on gallery_photos" ON gallery_photos;
CREATE POLICY "Admins can do everything on gallery_photos" ON gallery_photos FOR ALL 
USING (auth.uid() IS NOT NULL);

-- Promos (public read active)
DROP POLICY IF EXISTS "Public can view active promos" ON promos;
CREATE POLICY "Public can view active promos" ON promos FOR SELECT USING (true);

DROP POLICY IF EXISTS "Admins can do everything on promos" ON promos;
CREATE POLICY "Admins can do everything on promos" ON promos FOR ALL 
USING (auth.uid() IS NOT NULL);

-- Bookings
DROP POLICY IF EXISTS "Public can create bookings" ON bookings;
CREATE POLICY "Public can create bookings" ON bookings FOR INSERT WITH CHECK (true);

DROP POLICY IF EXISTS "Admins can view all bookings" ON bookings;
DROP POLICY IF EXISTS "Admins can update bookings" ON bookings;
CREATE POLICY "Admins can manage bookings" ON bookings FOR ALL 
USING (auth.uid() IS NOT NULL);

-- Profiles - simplified
DROP POLICY IF EXISTS "Enable read access for authenticated users" ON profiles;
DROP POLICY IF EXISTS "Enable update for users based on user_id" ON profiles;

CREATE POLICY "Anyone can read profiles" ON profiles FOR SELECT USING (true);
CREATE POLICY "Users can update own profile" ON profiles FOR UPDATE 
USING (auth.uid() = id);